services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: seido_data_hub
    ports:
      - "55432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
